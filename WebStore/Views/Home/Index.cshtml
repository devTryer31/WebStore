@using WebStore.Infrastructure.Utils.Pagination
@{
    ViewData["Title"] = "Home | E-Shopper";
}
@model IPaginator<ProductViewModel>

<partial name="Partial/_Slider" />

<section>
    <div class="container">
        <div class="row">
            <div class="col-sm-3">
                <partial name="Partial/_LeftSideBar" />
            </div>

            <div class="col-sm-9 padding-right">
                <partial name="Partial/_ProductsList" model="Model"/>

                <partial name="Partial/_CategoryTab" />

                <partial name="Partial/_RecomendedItems" />
            </div>
        </div>
    </div>
</section>